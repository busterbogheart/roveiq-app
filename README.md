## Ethan's dev skills test. Built with Svelte, Vite, and Tailwind CSS.

After cloning the repo, run `npm i`.  Then `npm start` will run the app.

Preview the production build using `npm run build && npm run preview`.

